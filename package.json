{
  "name": "knowledge-hub",
  "version": "1.0.0",
  "scripts": {
    "dev": "hugo serve",
    "build": "hugo --gc --minify"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
} 